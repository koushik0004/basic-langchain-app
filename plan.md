# Plan

Moved to [`.claude/plans/`](.claude/plans/README.md) for better Claude management.

Start at `.claude/plans/README.md` — the plan is split by domain (root, backend, frontend, run/verify, critical files).
